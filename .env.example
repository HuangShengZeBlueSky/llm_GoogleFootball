# OpenAI-compatible LLM endpoint（填写站点根路径，不要带 /chat/completions）
# 例: https://api.openai.com/v1 或你的代理地址
LLM_API_BASE=

# 必填: API Key
LLM_API_KEY=

# 模型名（需与服务端可用模型一致）
LLM_MODEL=gemini-3-flash

# Provider（可选）
# 可选值: openai_compatible | qwen | gemini_native
LLM_PROVIDER=openai_compatible
